package com.aichathub.app.ui.components

/**
 * Reusable premium UI components shared across all screens.
 *
 * Individual components are defined in their own files:
 * - AppCard.kt — standard card wrapper
 * - GradientHeaderCard.kt — gradient hero card
 * - GradientButton.kt — styled button with icon
 * - CompatibilityBadge.kt — model compatibility rating badge
 * - ModelIcon.kt — model purpose emoji icon
 * - StatusComponents.kt — MetricRow, ProgressBlock, StateDot
 * - StateComponents.kt — EmptyState, ErrorState, SectionHeader
 * - MessageEditDialog.kt — dialog for editing a sent message
 * - ChatSearchBar.kt — in-conversation search with next/previous navigation
 */
