package com.aichathub.app.domain.model

/**
 * Domain model types for AI Chat Hub.
 *
 * Individual types are defined in their own files within this package:
 * - CatalogModel.kt — model catalog entry
 * - CompatibilityLevel.kt — model-device compatibility rating
 * - ModelLifecycleState.kt — installed model lifecycle
 * - ModelFormat.kt — model file format
 * - ChatTemplate.kt — prompt template type
 * - DeviceProfile.kt — device hardware snapshot
 * - AiMemoryBudget.kt — safe memory budget
 * - Recommendation.kt — model recommendation result
 * - PerformanceSnapshot.kt — live inference metrics
 */
